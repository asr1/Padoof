Padoof notes

See if you can scrape from RPG Geek instead of the current place

Remove scrape

Remove referenes to videos,

Remove ffmpeg

Generate thumbnail from PDF cover

Set duration to page count

PDFs should have companies in their meta.

Default tags not appearings
  probably the difference between create and create adult! Take a look!

\# Add publisher to default parse tags (search for metaAssignedToVideos)